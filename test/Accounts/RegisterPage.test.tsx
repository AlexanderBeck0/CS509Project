import { describe, test } from "vitest";

describe("Register example", () => {
    test.todo("Create Account");
});

describe("Components Exist", () => {
    test.todo("Login link exists"); // There should be a link to Login somewhere
    test.todo("Search bar does not exist"); // Search bar should not exist in Register{age
});